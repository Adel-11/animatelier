const quizGuideBase =
  "# Quiz et révélations progressives\n\nAccessible dans l’application avec **Quiz**, et dans **Agents → Guide quiz**.\nUn agent commence par `window.animatelier.help()` : méthodes, guide, exemples\net schémas complets sont disponibles sans consulter le dépôt.\n\n## Contrat\n\n`compileQuiz(spec)` construit sans mutation un projet v2 validé et un calendrier\n`schedule` en secondes globales. `loadQuiz(spec)` le charge avec un nouvel ID\net une seule entrée dans l’historique. Une erreur laisse le projet intact.\n\n- `mode` : `choices` (QCM), `list` (liste cumulative), `cards` (question/réponse) ou `levels`.\n- `title` : 1 à 80 caractères.\n- `revealDelay` : 0,5 à 30 secondes, **3 par défaut**.\n- `answerDuration` : 0,5 à 30 secondes, **2 par défaut**, après révélation.\n- `questions` : contenu validé, avec réponse ou choix selon le mode.\n\n## Créativité pilotée par la spec\n\n- `layout` : `classic`, `game-show`, `minimal`, `presenter`.\n- `motion` : `fade`, `slide`, `pop`, `none`.\n- `progress` : `counter`, `bar`, `both`, `none`.\n- thèmes : `default`, `light`, `qff`, `game-show`, `neon`, `paper`, ou objet complet avec `extends`.\n- `presenter` contrôle nom, côté, couleurs, échelle, action et dialogue du personnage animé.\n- chaque question accepte `hint`, `explanation`, `points` et sa propre surcharge `theme`.\n\nLes sept exemples couvrent liste progressive, QCM, jeu télévisé, présentateur, cartes mémoire, direction artistique et défi vertical à niveaux. Le mode `levels` ajoute formats verticaux, images, effets, intro/outro, script vocal et budget de durée.\n\n## Workflow agent\n\nUtiliser `compileQuiz(spec)` pour prévisualiser sans mutation, puis `loadQuiz(spec)` pour charger. Le MCP expose le même contrat via `quiz_compile`, puis `project_load_data`. Toutes les sorties sont des primitives v2 éditables avec les commandes normales ; aucun HTML ou SVG arbitraire n’est exécuté.\n\n## Limites\n\nLe JSON contient les réponses : il produit une animation, pas un jeu secret interactif. Inspecter le rendu aux instants de question et de révélation, puis sauvegarder ou exporter.\n";

export const quizGuide = quizGuideBase;
